---
// Signup Modal
// ------------
// Description: A modal with a form to sign up.

// Components
// - UI
import Modal from '../../ui/Modal.astro'

// Props
// Props
type Props = {
	id?: string
	title?: string
	classes?: string
	description?: string
	image?: any
}
const { id, title, description, image, classes } = Astro.props
---

<Modal id={id} classes={classes} title={title} image={image}>
	<!-- <Image src={image} alt="School Image" class="mb-4 h-48 w-full object-cover" /> -->
	<div class="basic-text" set:html={description} />
</Modal>
